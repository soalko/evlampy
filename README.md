# Evlampy Messenger

## О проекте

**Evlampy** — это учебный клиент-серверный мессенджер на C++ с шифрованием сообщений и модульной архитектурой. Проект демонстрирует разбиение приложения на логические компоненты, использование CMake, подключение внешних библиотек, а также организацию unit-тестов и сценарных тестов.

В проекте реализованы:

- регистрация и авторизация пользователей;
- обмен личными и групповыми сообщениями;
- шифрование сообщений на стороне клиента;
- хранение истории переписки и статусов сообщений;
- управление контактами и группами;
- минимальный графический интерфейс для работы с мессенджером;
- набор unit-тестов и тестовых сценариев для отладки.

## Структура репозитория

```text
include/        публичные заголовочные файлы
src/            исходный код приложения
  chat/         клиент, сервер, сообщение, сценарий main
  encryption/   стратегия шифрования и фабрика ключей
  network/      транспортный слой и протокол обмена
  patterns/     паттерны Command, Singleton и др.
  gui/          графический интерфейс
tests/          unit-тесты, сценарии, тестовые утилиты и документация
build/          конфигурация основного CMake-проекта в текущем workspace
build-tests/    отдельная директория для сборки тестов
build-gui/      отдельная директория для сборки GUI-версии
```

## Используемые технологии и библиотеки

- **C++17** — основной язык проекта;
- **OpenSSL** — криптография, RSA и AES-GCM;
- **Threads** — многопоточность и синхронизация;
- **GoogleTest** — unit-тестирование;
- **Cocoa / AppKit** — основной минимальный графический интерфейс для macOS;
- **Qt 6 Widgets/Core/Gui** — альтернативная GUI-ветка для не-macOS платформ.

## Сборка проекта

### Основное приложение

Если проект открыт как обычный CMake-проект, используйте стандартную out-of-source-сборку:

```bash
cmake -S . -B build
cmake --build build -j4
```

После сборки будет доступен исполняемый файл `target_exec`.

### Если CMake не находит OpenSSL

Проект использует `OpenSSL::Crypto`. На macOS обычно достаточно установить пакет Homebrew:

```bash
brew install openssl@3
```

При необходимости можно явно передать путь в CMake:

```bash
cmake -S . -B build -DOPENSSL_ROOT_DIR="$(brew --prefix openssl@3)"
cmake --build build -j4
```

### Сборка тестов

Для тестов используется отдельная директория:

```bash
cmake -S . -B build-tests -DBUILD_TESTS=ON
cmake --build build-tests -j4
```

### Сборка GUI

В проекте предусмотрен минимальный графический интерфейс. В зависимости от платформы используется один из вариантов:

- `src/gui/MessengerWindow.cpp` — Qt-версия GUI;
- `src/gui/main_gui.mm` — macOS-версия на Cocoa/AppKit.

Если у вас настроена отдельная GUI-сборка, используйте отдельную директорию, например:

```bash
cmake -S . -B build-gui -DBUILD_GUI=ON
cmake --build build-gui -j4
```

## Сборка и запуск в Docker

Для контейнеризации используется многостадийная сборка: на этапе `builder` ставятся компилятор, CMake, OpenSSL, GoogleTest и Qt 6, а в финальный образ попадают только готовые бинарники без временных файлов сборки.

### Сборка образа

```bash
docker build -t evlampy-messenger .
```

Если нужно отключить GUI-сборку, можно передать:

```bash
docker build --build-arg BUILD_GUI=OFF -t evlampy-messenger .
```

### Запуск основного сценария

Аргументы командной строки передаются в `target_exec` напрямую:

```bash
docker run --rm evlampy-messenger
docker run --rm evlampy-messenger --message "Hello from Docker"
docker run --rm evlampy-messenger --alice-name ivan --bob-name pavel
```

### Запуск тестов внутри контейнера

```bash
docker run --rm evlampy-messenger test
```

### Запуск GUI-варианта

Для Qt-версии можно использовать режим `gui`. Если дисплей недоступен, контейнер автоматически переходит в `QT_QPA_PLATFORM=offscreen`:

```bash
docker run --rm evlampy-messenger gui
```

Если у вас есть X11/Display, можно также пробросить его в контейнер и запускать GUI в обычном режиме.

## Запуск

### Консольная версия

```bash
./build/target_exec
```

Консольный сценарий демонстрирует:

- регистрацию двух пользователей;
- вход в систему;
- отправку личного сообщения;
- создание группы;
- отправку группового сообщения;
- удаление сообщения для всех участников.

### Графический интерфейс

```bash
./build-gui/messenger_gui
```

Если вы запускаете GUI-сборку на macOS, приложение открывает минимальное окно мессенджера.

**Как использовать GUI:**

1. **Регистрация**: введите уникальное имя и пароль, нажмите **Register** (в первый раз).
   - Генерируется локальная пара RSA-ключей в `~/.evlampy/keys/`.
2. **Логин**: введите то же имя и пароль, нажмите **Login**.
   - Ключи автоматически загружаются с диска.
3. **Создание чата**: нажмите **New chat** и введите имя другого пользователя (если он зарегистрирован).
4. **Отправка сообщения**: выберите чат, введите текст в поле, нажмите **Send**.
5. **Создание группы**: нажмите **Create group**, укажите название и участников (через запятую).
6. **Удаление сообщения**: выберите сообщение и нажмите **Delete**, оно удалится у всех участников.
7. **Logout**: нажмите **Logout**, чтобы выйти.


## Тестирование

### Unit-тесты

Unit-тесты находятся в папке `tests/unit/` и покрывают основные классы проекта:

- `Message`
- `MessageProtocol`
- `TcpClient`
- `TcpServer`
- `AES256GCMStrategy`
- `OpenSSLKeyFactory`
- `ChatServer`
- `ChatClient`
- `Observer`
- `Command` и `Logger`

Запуск всех тестов:

```bash
cd build-tests
ctest --output-on-failure
```

### Тестовые сценарии

Сценарные мини-программы находятся в `tests/scenarios/` и моделируют ключевые этапы работы системы:

- регистрация и авторизация;
- отправка личного сообщения;
- создание и использование группового чата;
- удаление сообщения для всех;
- административные действия;
- повторный вход после перезапуска;
- **многопользовательский сценарий в одном процессе** (симуляция GUI с переключением между пользователями).

## Примечания к GUI

При работе с графическим интерфейсом:

- При регистрации каждого пользователя генерируется и сохраняется локальная пара RSA-ключей в `~/.evlampy/keys/`.
- При логине ключи загружаются автоматически.
- Интеграционный тест `scenario_multi_user_gui` проверяет корректность работы при переключении между пользователями в одном и том же процессе приложения.


## Пример сценария работы

1. Пользователь регистрируется и получает локальную пару ключей.
2. Пользователь входит в систему по логину и паролю.
3. Клиент создаёт защищённое сообщение и отправляет его адресату.
4. Сервер сохраняет зашифрованные данные и пересылает их получателю.
5. Получатель расшифровывает сообщение локально на своей стороне.

## Примечания

- Приватные ключи пользователей не должны передаваться по сети.
- История сообщений и данные пользователей хранятся в зашифрованном или защищённом виде в соответствии с моделью проекта.
- Проект демонстрационный и предназначен для изучения архитектурных решений, паттернов проектирования, CMake и unit-тестирования.
